{
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch Script",
      "program": "${file}",
      "preLaunchTask": "tsc: build - tsconfig.json",
      "outFiles": [
        "${workspaceFolder}/out/**/*.js"
      ],
      "console": "integratedTerminal"
    }
  ]
}